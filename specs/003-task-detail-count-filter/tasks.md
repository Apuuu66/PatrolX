---
description: "任务详情统计数字过滤实现任务列表"
---

# 任务：任务详情统计数字过滤

**输入**：来自 `/specs/003-task-detail-count-filter/` 的设计文档

**前置条件**：plan.md、spec.md、research.md、data-model.md、quickstart.md

**测试**：包含前端单元测试任务，验证过滤逻辑和数量一致性。

**组织方式**：任务按用户故事分组，支持独立实现和测试。

## 格式：`[ID] [P?] [故事] 描述`

- **[P]**：可并行执行（不同文件，无依赖）
- **[故事]**：该任务所属的用户故事（例如 US1、US2、US3）
- 描述中包含精确的文件路径

---

## 阶段 1：设置

**目的**：确认现有代码可编译、测试可通过

- [x] T001 运行 `cd web && npm run build` 确认前端构建通过
- [x] T002 [P] 运行 `cd web && npm test` 确认现有测试通过

**检查点**：基线确认——变更前构建和测试均通过

---

## 阶段 2：用户故事 1 - 点击状态数量后只看对应规则（P1）🎯 MVP

**目标**：总览区状态数量可点击，点击后全局过滤所有分类分组中对应状态的规则。

**独立测试**：打开包含多种状态规则的任务详情，点击某一状态数量，确认列表只显示该状态的规则。

### 用户故事 1 的测试

- [x] T003 [P] [US1] 在 `web/src/pages/TaskDetailPage.test.tsx` 中编写状态过滤单元测试：点击"告警"数量后列表只显示告警规则、无匹配分类隐藏、数量为 0 时显示空状态

### 用户故事 1 的实现

- [x] T004 [P] [US1] 修改 `web/src/components/SummaryCards.tsx`：为状态数量卡片添加可选的 `onStatusClick` 回调属性和 `activeStatus` 属性，点击时调用回调并传递状态 key；当前激活状态卡片增加视觉反馈（边框/阴影变化）
- [x] T005 [US1] 修改 `web/src/pages/TaskDetailPage.tsx`：添加 `statusFilter` 状态（类型 `RuleStatus | null`）；从可见规则列表推导各状态数量并传递给 `SummaryCards`；点击状态数量时设置 `statusFilter` 为该状态；使用 `statusFilter` 过滤 `rules` 列表后再进行分类分组（依赖 T004）

**检查点**：点击状态数量可过滤规则列表，MVP 可独立验证

---

## 阶段 3：用户故事 2 - 取消状态过滤并恢复全量列表（P1）

**目标**：再次点击已激活的状态数量时取消过滤，恢复显示全部可见规则。

**独立测试**：先点击一个状态数量完成过滤，再点击同一个状态数量，确认列表恢复全量。

### 用户故事 2 的测试

- [x] T006 [P] [US2] 在 `web/src/pages/TaskDetailPage.test.tsx` 中编写取消过滤测试：再次点击同一状态取消过滤、点击不同状态替换过滤、轮询刷新后过滤意图保持

### 用户故事 2 的实现

- [x] T007 [US2] 修改 `web/src/pages/TaskDetailPage.tsx`：点击已激活状态时将 `statusFilter` 设为 `null`（取消过滤）；点击不同状态时替换 `statusFilter`；轮询刷新数据时保留 `statusFilter` 不变（依赖 T005）

**检查点**：设置、取消、替换和刷新保持均可独立验证

---

## 阶段 4：用户故事 3 - 数量与过滤后的列表保持一致（P2）

**目标**：总览状态数量始终由可见规则列表推导；分类分组标题数量反映当前过滤范围。

**独立测试**：验证过滤前后总览数字不变、分组标题数字随过滤变化、总览数字不包含隐藏规则。

### 用户故事 3 的测试

- [x] T008 [P] [US3] 在 `web/src/pages/TaskDetailPage.test.tsx` 中编写数量一致性测试：总览状态数量由可见规则列表推导而非 `task.stats`、分类标题数量随过滤变化、总览数字不因过滤而改变

### 用户故事 3 的实现

- [x] T009 [US3] 修改 `web/src/pages/TaskDetailPage.tsx`：将总览状态数量的数据源从 `task.stats` 改为由可见规则列表按状态统计推导（依赖 T005）；确认分类分组标题中的数量使用过滤后的列表长度（依赖 T005）

**检查点**：总览数字、分组标题数字和实际展示条数三者一致

---

## 阶段 5：收尾与横切关注点

**目的**：最终验证和构建检查

- [x] T010 运行 `cd web && npm run build` 确认前端构建通过
- [x] T011 运行 `cd web && npm test` 确认所有前端测试通过
- [x] T012 运行 `make lint` 确认代码风格通过
- [x] T013 按 `specs/003-task-detail-count-filter/quickstart.md` 手动验证 5 个场景

---

## 依赖与执行顺序

### 阶段依赖

- **阶段 1（设置）**：无依赖，可立即开始
- **阶段 2（US1）**：依赖阶段 1 基线确认
- **阶段 3（US2）**：依赖 T005 完成
- **阶段 4（US3）**：依赖 T005 完成
- **阶段 5（收尾）**：依赖所有前置阶段完成

### 用户故事依赖

- **US1（P1）**：核心过滤能力，必须先完成
- **US2（P1）**：依赖 US1 的过滤状态管理（T005）
- **US3（P2）**：依赖 US1 的可见规则推导（T005）

### 并行机会

- T003、T006、T008 为不同测试场景，可在对应实现完成后并行编写
- T004 为独立组件修改，可与 T005 的测试并行准备
- 阶段 3 和阶段 4 均依赖 T005，但两者修改同一文件 `TaskDetailPage.tsx`，建议按顺序执行

---

## 实现策略

### MVP 优先

1. 完成阶段 1 基线确认
2. 完成阶段 2（US1）：状态过滤核心能力
3. **停止并验证**：手动测试场景 1
4. 继续阶段 3（US2）和阶段 4（US3）
5. 阶段 5 最终验证
